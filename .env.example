# Environment Variables for Resume Reviewer

# GitHub Token (Optional - for analyzing candidate GitHub profiles)
# Get your token from: https://github.com/settings/tokens
# Required scopes: public_repo (or no scopes for public data only)
GITHUB_TOKEN=

# Database Configuration (used in Docker)
DATABASE_HOST=postgres
DATABASE_PORT=5432
DATABASE_NAME=ResumeReviewer
DATABASE_USER=postgres
DATABASE_PASSWORD=1234

# Ollama Configuration
OLLAMA_BASE_URL=http://ollama:11434
EMBEDDING_MODEL=nomic-embed-text
LLM_MODEL=qwen3:latest

# Application Paths
DATASET_ROOT=/app/dataset
CHROMA_DB_PATH=/app/chroma_db
CACHE_DIR=/app/.cache
OUTPUT_DIR=/app/output
